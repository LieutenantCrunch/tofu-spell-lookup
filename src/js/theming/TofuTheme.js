import { createTheme } from '@mui/material/styles';

export const TofuTheme = createTheme({
    palette: {
        mode: 'dark',
        background: {
            default: '#00222c'
        }
    }
});
